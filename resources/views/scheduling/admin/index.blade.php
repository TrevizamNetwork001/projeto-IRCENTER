@extends('layouts.app') @section('title','Agenda — IRCENTER') @section('content')<div class="page-header"><div><span class="eyebrow">Operação</span><h1>Agenda</h1><p>Agendamentos e disponibilidade.</p></div><a class="button" href="{{ route('scheduling.admin.export',request()->query()) }}">Exportar CSV</a></div>@include('scheduling.admin.nav')<section class="scheduling-metrics"><article><span>Hoje</span><strong>{{ $today }}</strong></article><article><span>Próximos 7 dias</span><strong>{{ $nextSeven }}</strong></article><article><span>Cancelados no mês</span><strong>{{ $cancelled }}</strong></article><article><span>No-show</span><strong>{{ $noShow }}</strong></article></section><form method="get" class="filters"><input name="search" value="{{ request('search') }}" placeholder="Nome ou e-mail"><select name="status"><option value="">Todos os status</option>@foreach(['scheduled'=>'Agendado','confirmed'=>'Confirmado','cancelled'=>'Cancelado','completed'=>'Concluído','no_show'=>'Não compareceu'] as $v=>$l)<option value="{{ $v }}" @selected(request('status')===$v)>{{ $l }}</option>@endforeach</select><select name="event_type"><option value="">Todos os serviços</option>@foreach($eventTypes as $type)<option value="{{ $type->id }}" @selected(request('event_type')==$type->id)>{{ $type->name }}</option>@endforeach</select><input type="date" name="from" value="{{ request('from') }}"><input type="date" name="to" value="{{ request('to') }}"><button class="button">Filtrar</button></form><div class="table-wrap"><table><thead><tr><th>Data/hora</th><th>Visitante</th><th>Serviço</th><th>Status</th><th>Ações</th></tr></thead><tbody>@forelse($appointments as $a)<tr><td>{{ $a->scheduled_start_at->setTimezone($a->timezone)->format('d/m/Y H:i') }}</td><td>{{ $a->guest_name }}<small>{{ $a->guest_email }}</small></td><td>{{ $a->eventType->name }}<small>{{ $a->eventType->duration_minutes }} min</small></td><td><span class="status-badge">{{ $a->statusLabel() }}</span></td><td><a href="{{ route('scheduling.admin.appointments.show',$a) }}">Detalhes</a></td></tr>@empty<tr><td colspan="5" class="empty-state">Nenhum agendamento encontrado.</td></tr>@endforelse</tbody></table></div>{{ $appointments->links() }}@endsection
