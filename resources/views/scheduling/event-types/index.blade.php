@extends('layouts.app') @section('content')<div class="page-header"><div><h1>Tipos de agendamento</h1><p>Serviços publicados e suas regras.</p></div></div>@include('scheduling.admin.nav')@if(auth()->user()->canOperate())<details class="panel"><summary>Novo tipo</summary><form method="post" action="{{ route('scheduling.admin.event-types.store') }}" class="form-grid">@csrf @include('scheduling.event-types.fields',['type'=>null])<button class="button button-primary">Criar tipo</button></form></details>@endif<div class="card-grid">@forelse($eventTypes as $type)<article class="panel"><h2>{{ $type->name }}</h2><p>{{ $type->duration_minutes }} min · {{ $type->active?'Ativo':'Inativo' }}</p><p><a href="{{ route('scheduling.public.show',$type) }}" target="_blank" rel="noopener">/agenda/{{ $type->slug }}</a></p><div class="page-actions"><a class="button" href="{{ route('scheduling.public.show',$type) }}" target="_blank" rel="noopener">Ver página pública</a><a class="button" href="{{ route('scheduling.admin.availability',$type) }}">Disponibilidade</a></div>@if(auth()->user()->canOperate())<details><summary>Editar</summary><form method="post" action="{{ route('scheduling.admin.event-types.update',$type) }}" class="form-grid">@csrf @method('PUT') @include('scheduling.event-types.fields',['type'=>$type])<button class="button button-primary">Salvar</button></form></details>@endif</article>@empty<p class="empty-state">Nenhum tipo de agendamento cadastrado.</p>@endforelse</div>@endsection
