@extends('scheduling.public.layout') @section('content')<article class="confirmation-card"><h1>Cancelar agendamento?</h1><p>{{ $appointment->scheduled_start_at->setTimezone($appointment->timezone)->format('d/m/Y H:i') }} — {{ $appointment->timezone }}</p><form method="post">@csrf<input type="hidden" name="token" value="{{ request('token') }}"><button class="button button-danger">Confirmar cancelamento</button></form></article>@endsection
