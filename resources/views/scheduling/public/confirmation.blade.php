@extends('scheduling.public.layout') @section('title','Agendamento confirmado — IRCENTER') @section('content')<article class="confirmation-card"><span class="confirmation-icon" aria-hidden="true">✓</span><h1>{{ $appointment->status==='cancelled'?'Agendamento cancelado':'Agendamento confirmado' }}</h1><dl><div><dt>Serviço</dt><dd>{{ $appointment->eventType->name }}</dd></div><div><dt>Data e hora</dt><dd>{{ $appointment->scheduled_start_at->setTimezone($appointment->timezone)->format('d/m/Y H:i') }}</dd></div><div><dt>Timezone</dt><dd>{{ $appointment->timezone }}</dd></div><div><dt>Duração</dt><dd>{{ $appointment->eventType->duration_minutes }} minutos</dd></div></dl>@if($appointment->status!=='cancelled')<a class="button button-primary" href="{{ route('scheduling.public.ics',$appointment) }}">Adicionar ao calendário (.ics)</a>@endif</article>@endsection
